Place a CSV called panels.csv in the same folder as the exe and run it

The CSV must contain a field with an IP address with the header "IP"

Tool will ping every IP and write a new CSV of the devices that fail
